<section class="container">
    <header class="d-flex flex-column align-items-center mb-5">
        <h1 class="mt-4 mb-4 text-center">Learn from the best.</h1>
        <h2 class="text-center fs-4 text-body-secondary">Interact with the fullstack community today.</h2>
        {{#unless loggedIn}}
        <a href="/login" class="btn btn-lg btn-primary mt-3 mb-3">Sign Up</a>
        {{/unless}}
    </header>

    {{> blogPostGrid}}
</section>